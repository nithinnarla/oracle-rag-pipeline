# ORACLE, Optimized Retrieval Augmented Clinical and Lay-language Enquiry
# Requirements for personalized health literacy RAG pipeline
# Python 3.10+

# Core ML and NLP
torch==2.10.0
transformers==4.57.6
datasets==2.19.0

# Retrieval and vector store

# PEFT/LoRA adapter architecture (built and structurally validated, not trained to convergence)
peft==0.20.0

# Biomedical embeddings
# BioSentVec loaded via HuggingFace Hub (no pip install needed)
# APPLS metrics loaded from Guo et al. evaluation code (no pip install, clone from github.com/xiaolan98/APPLS)

# OpenAI
openai==2.29.0

# Evaluation metrics
rouge-score==0.1.2
bert-score==0.3.13

# Readability scoring
textstat==0.7.3

# Data handling
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.4.2
scipy==1.13.0

# Visualization
matplotlib==3.8.4
pillow==10.3.0
seaborn==0.13.2

# Utilities
tqdm==4.66.4
requests==2.31.0

# Pinned in an earlier build but imported nowhere in src/ or notebooks/, so they are
# not installed for a reproduction run. Kept here as a record of the planned stack:
# FAISS indexing, BM25 hybrid fusion, LangChain orchestration and BioSentVec
# embeddings were designed but never implemented; retrieval is DPR with cosine
# similarity over numpy arrays. faiss-cpu in particular conflicts with the
# PlainQAFact environment, as noted in factual_consistency_eval.py.
# sentence-transformers==2.7.0
# faiss-cpu==1.8.0
# langchain==0.1.20
# langchain-community==0.0.38
# langchain-openai==0.1.6
# rank-bm25==0.2.2
# nltk==3.8.1
# sacrebleu==2.4.0
# python-dotenv==1.0.1
